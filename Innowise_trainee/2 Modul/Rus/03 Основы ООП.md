# Основы ООП

## Table of Contents

- [[#Классы и объекты]]
- [[#Конструкторы]]
- [[#Ключевое слово this]]
- [[#Статические члены (static)]]
- [[#Инкапсуляция и модификаторы доступа]]
- [[#Геттеры и сеттеры]]
- [[#Метод toString]]
- [[#equals() и hashCode()]]

**Related notes:** [[AQA Java rus]]

> [!info] Видео-курс (Заур Трегулов)
> Курс 1 (OCA): L4 классы и объекты, L5 конструкторы и методы, L6 перегрузка и this, L8 static/final, L9 область видимости, L11 передача по ссылке.

---

## Классы и объекты

**Класс** — это шаблон. **Объект** — экземпляр класса.

```java
public class Car {
    String brand;
    String color;
    int year;
    void startEngine() {
        System.out.println("Двигатель " + brand + " запущен");
    }
}

Car myCar = new Car();
myCar.brand = "Toyota";
myCar.startEngine();
```

### Анатомия класса

| Часть | Что делает |
|---|---|
| **Поля** | Хранят состояние объекта |
| **Конструкторы** | Инициализируют объект при создании |
| **Методы** | Определяют поведение |
| **Статические члены** | Принадлежат классу, а не объекту |

---

## Конструкторы

```java
public class Student {
    String name;
    int age;

    public Student(String name, int age) {
        this.name = name;
        this.age = age;
    }

    public Student() {
        this("Неизвестно", 0);   // цепочка конструкторов
    }
}

Student s = new Student("Alice", 20);
```

> [!tip] Цепочка конструкторов
> `this(...)` вызывает один конструктор из другого. Должен быть первой строкой.

---

## Ключевое слово this

`this` ссылается на текущий объект.

```java
public class Employee {
    String name;
    public Employee(String name) {
        this.name = name;   // this.name = поле, name = параметр
    }
}
```

**Три用途:** 1) различить поле и параметр 2) вызвать другой конструктор 3) вернуть текущий объект.

---

## Статические члены (static)

Принадлежат классу, а не объектам. Загружаются **один раз** при первом обращении к классу.

### Static-поля

```java
public class Counter {
    static int totalCount = 0;   // общее для всех объектов
    int instanceCount = 0;       // своё для каждого объекта

    public Counter() {
        totalCount++;
        instanceCount++;
    }
}

Counter c1 = new Counter();  // totalCount = 1, instanceCount = 1
Counter c2 = new Counter();  // totalCount = 2, instanceCount = 1
System.out.println(Counter.totalCount);   // 2 — обращение через класс
```

### Static-методы

```java
public class MathUtils {
    public static int add(int a, int b) {
        return a + b;
    }
}

int sum = MathUtils.add(5, 3);   // 8 — вызов без создания объекта
```

> [!info] Static vs Instance
> - Static-методы могут обращаться **только** к static-полям напрямую
> - Instance-методы могут обращаться и к static, и к instance полям
> - `this` нельзя использовать в static-методах (нет объекта)

### Static-блоки

Static-блок выполняется **один раз** при загрузке класса — до создания любых объектов.

```java
public class Config {
    static String dbUrl;
    static Properties props;

    static {
        System.out.println("Загрузка конфигурации...");
        dbUrl = "jdbc:mysql://localhost:3306/app";
        // props.load(...) — загрузка файла
    }
}
```

**Используется для:** инициализации сложных static-полей, загрузки конфигов, подключения к БД.

### Порядок инициализации

Когда JVM впервые загружает класс, всё выполняется **строго сверху вниз**:

```java
public class Demo {
    static int a = 1;          // 1. static поля
    static {                   // 2. static блоки
        System.out.println("static: a=" + a + ", b=" + b);
    }
    static int b = 2;          // 3. static поля (продолжение)
    int x = 3;                 // 4. instance поля
    {                          // 5. instance блоки
        System.out.println("instance: x=" + x);
    }
    public Demo() {            // 6. конструктор
        System.out.println("constructor");
    }
}

// При new Demo():
// static: a=1, b=0   ← b ещё не инициализирован!
// instance: x=3
// constructor
```

**Общий порядок загрузки объекта:**
```
1. static поля (сверху вниз)
2. static блоки (сверху вниз)
3. instance поля (сверху вниз)
4. instance блоки (сверху вниз)
5. конструктор
```

### При наследовании

```
1. static родителя (все static поля + блоки)
2. static потомка  (все static поля + блоки)
3. instance родителя → конструктор родителя
4. instance потомка → конструктор потомка
```

```java
class Parent {
    static { System.out.println("Parent static"); }
    { System.out.println("Parent instance"); }
    Parent() { System.out.println("Parent constructor"); }
}

class Child extends Parent {
    static { System.out.println("Child static"); }
    { System.out.println("Child instance"); }
    Child() { System.out.println("Child constructor"); }
}

// new Child() выведет:
// Parent static
// Child static
// Parent instance
// Parent constructor
// Child instance
// Child constructor
```

> [!warning] Важно
> Проверяемые исключения внутри static-блоков должны быть пойманы внутри блока. Непроверяемые исключения оборачиваются в `ExceptionInInitializerError`.
> Порядок полей и блоков в коде **имеет значение** — JVM выполняет строго сверху вниз.

---

## Инкапсуляция и модификаторы доступа

| Модификатор | Класс | Пакет | Подкласс | Все |
|---|---|---|---|---|
| `public` | ✅ | ✅ | ✅ | ✅ |
| `protected` | ✅ | ✅ | ✅ | ❌ |
| (по умолч.) | ✅ | ✅ | ❌ | ❌ |
| `private` | ✅ | ❌ | ❌ | ❌ |

---

## Геттеры и сеттеры

```java
public class User {
    private String username;
    private int age;

    public String getUsername() { return username; }

    public void setUsername(String username) {
        if (username != null && username.length() >= 3) {
            this.username = username;
        }
    }
}
```

---

## Метод toString

```java
@Override
public String toString() {
    return "Product{name='" + name + "', price=" + price + "}";
}
```

---

## equals() и hashCode()

По умолчанию `equals()` из `Object` сравнивает **ссылки** (как `==`). Если нужно сравнивать **содержимое**, его надо переопределить.

### Зачем переопределять equals()?

```java
User a = new User("Alice", 25);
User b = new User("Alice", 25);

System.out.println(a == b);       // false — разные объекты
System.out.println(a.equals(b));  // false — equals() по умолчанию использует ==
```

После переопределения:

```java
@Override
public boolean equals(Object o) {
    if (this == o) return true;
    if (o == null || getClass() != o.getClass()) return false;
    User user = (User) o;
    return age == user.age && Objects.equals(name, user.name);
}
```

Теперь `a.equals(b)` вернёт `true`, потому что содержимое совпадает.

### Зачем переопределять hashCode() тоже?

**Контракт** гласит: если два объекта равны (`equals()` возвращает `true`), у них должен быть одинаковый `hashCode()`. Это требование для `HashMap`, `HashSet` и любых hash-коллекций.

```java
// Без переопределения hashCode():
Set<User> users = new HashSet<>();
users.add(new User("Alice", 25));
users.contains(new User("Alice", 25));  // false! — разный hashCode

// С правильным hashCode():
@Override
public int hashCode() {
    return Objects.hash(name, age);
}
// Теперь contains() вернёт true
```

**Что сломается, если переопределить только equals():**
- `HashMap` использует `hashCode()` для поиска корзины (bucket). Если у двух равных объектов разные хеш-коды, map положит их в разные корзины и никогда не найдёт один по другому.
- `HashSet` работает по тому же принципу — "равные" объекты выглядят как дубликаты.

> [!danger] Контракт
> 1. Если `a.equals(b)` — `true`, то `a.hashCode() == b.hashCode()` обязан быть `true`
> 2. Если хеш-коды равны, `equals()` может быть и `true`, и `false` (коллизии допустимы)
> 3. Если `a.equals(b)` — `false`, хеш-коды *могут* совпадать (но лучше бы отличались для производительности)

> [!tip] IntelliJ генерирует оба
> `Alt + Insert` → **equals() and hashCode()** → выбрать поля. Всегда генерируй их вместе.

---

## Вопросы для собеседования

### Топ-10

**1. В чём разница между классом и объектом?**
Класс — чертёж (шаблон), описывающий структуру и поведение. Объект — конкретный экземпляр класса, созданный через `new`. Один класс может порождать много независимых объектов.

**2. Что такое конструктор? Может ли он возвращать значение?**
Конструктор — специальный метод, инициализирующий объект при создании. Имя совпадает с именем класса. Нет возвращаемого типа (даже `void`). Не может возвращать значение.

**3. Что такое перегрузка конструкторов и цепочка конструкторов?**
Перегрузка — несколько конструкторов с разными параметрами. Цепочка — вызов одного конструктора из другого через `this(...)`, чтобы не дублировать код.

**4. На что ссылается ключевое слово this?**
`this` ссылается на текущий объект. Используется для: (1) различия поля и параметра, (2) вызова другого конструктора, (3) возврата текущего объекта.

**5. В чём разница между static и обычными (instance) членами?**
Static принадлежат классу, общие для всех объектов. Доступ через имя класса. Instance — принадлежат каждому объекту отдельно. Static методы не могут напрямую обращаться к instance полям.

**6. Что такое инкапсуляция? Зачем она нужна?**
Инкапсуляция — сокрытие внутренних данных и предоставление доступа только через публичные методы. Защищает состояние объекта (нельзя установить `user.age = -5`). Уменьшает связанность кода.

**7. Какие модификаторы доступа есть в Java?**
`private` (только класс), default/package-private (пакет), `protected` (пакет + наследники), `public` (все).

**8. Зачем нужны геттеры и сеттеры?**
Геттеры — контролируемое чтение. Сеттеры — контролируемая запись с валидацией (`setAge(-5)` будет отклонён). Позволяют менять внутреннюю реализацию без изменения внешнего кода.

**9. Что такое метод toString? Зачем его переопределять?**
`toString()` наследуется от `Object` и возвращает `ClassName@hashcode`. Переопределение даёт читаемое представление объекта для отладки и логирования.

**10. Что такое статическая константа? Как объявляется?**
Константа уровня класса, значение нельзя изменить. `public static final TYPE NAME = value;`. Имя — UPPER_SNAKE_CASE.

---

### Хитрые вопросы

**1. Что будет, если не написать ни одного конструктора?**
Java добавит конструктор по умолчанию без параметров. Он инициализирует поля значениями по умолчанию (0, null, false).

**2. Что будет, если написать хотя бы один конструктор с параметрами?**
Конструктор по умолчанию исчезнет. Если нужен ещё и без параметров — его нужно написать явно. Частая ошибка — наследовать класс, у родителя только параметризованный конструктор, и получить ошибку компиляции.

**3. Может ли static метод обращаться к нестатическим полям?**
Нет. Static метод не привязан к объекту. Ему неоткуда взять instance поля. Ошибка компиляции: "Cannot make a static reference to the non-static field".

**4. Можно ли использовать this в static методе?**
Нет. `this` ссылается на текущий объект. В static методах объекта нет.

**5. Чем отличаются public поля с прямым доступом от private полей с геттерами/сеттерами?**
Public поля — нет контроля, `product.price = -100;` работает. Геттеры/сеттеры позволяют добавить валидацию, логирование, триггеры. Можно изменить внутреннюю реализацию, не ломая внешний код.

