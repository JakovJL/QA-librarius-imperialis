# Normalize line endings to LF in the repo so Windows <-> Android stay in sync
* text=auto eol=lf

# Treat known binary assets as binary (no line-ending munging)
*.pptx binary
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.canvas text eol=lf
